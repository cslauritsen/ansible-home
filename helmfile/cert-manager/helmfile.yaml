repositories:
  - name: jetstack
    url: https://charts.jetstack.io
releases:
  - name: cert-manager
    chart: jetstack/cert-manager
    version: 1.17.0
    namespace: cert-manager
    createNamespace: true
    values:
      - crds:
          enabled: true